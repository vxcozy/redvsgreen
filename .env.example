# Copy to .env.local and fill in values
# Database (Vercel Postgres — auto-populated via vercel env pull)
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# Auth.js — generate secret: openssl rand -base64 32
AUTH_SECRET=

# Google OAuth
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Twitter/X OAuth 2.0
AUTH_TWITTER_ID=
AUTH_TWITTER_SECRET=
